# REST API INFORMATION
spring.application.name=support_link
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/support_link?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.port=8081

# JWT Configuration
jwt.secret=verysecretkeythatislongenoughforhs512algorithmverysecretkeythatislongenoughforhs512algorithm
jwt.expiration=86400000

